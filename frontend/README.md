The prototype runs without Node by using the prebuilt static dashboard in `frontend/dist/`.

For React development, install Node.js and run:
`npm install`
`npm run dev`

The production-style prototype itself is served by FastAPI at `/`.

version: "3.8"

services:
  rag-assistant:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: rag-cybersecurity-assistant
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
    restart: unless-stopped
